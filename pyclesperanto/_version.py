# pyclesperanto version
VERSION_CODE = (0, 6, 5)
VERSION_STATUS = ""
VERSION = ".".join(str(x) for x in VERSION_CODE) + VERSION_STATUS

# clic version
CLIC_VERSION_CODE = (0, 6, 5)
CLIC_VERSION_STATUS = ""
CLIC_VERSION = ".".join(str(x) for x in CLIC_VERSION_CODE) + CLIC_VERSION_STATUS
